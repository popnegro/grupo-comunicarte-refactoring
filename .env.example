# Database Connection URL (PostgreSQL / Neon)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/grupo_comunicarte

# Server Port (default 3000)
PORT=3000

# Node Environment (development / production)
NODE_ENV=development
