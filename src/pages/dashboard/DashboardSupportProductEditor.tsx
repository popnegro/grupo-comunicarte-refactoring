import { useEffect, useMemo, useState } from 'react';
import { ArrowLeft, Eye, ImagePlus, Save, Trash2, Video } from 'lucide-react';
import { useNavigate, useParams } from 'react-router-dom';
import { DashboardShell } from '../../components/dashboard/DashboardShell';
import { SupportCard } from '../../components/inventory/SupportCard';
import { apiFetch } from '../../lib/api';
import { getSupportEditorThemeConfig } from './supportEditorThemes';
import type { InventoryItem, MobileRoute, SupportFamily } from '../../types';

type SupportType = 'tradicional' | 'led' | 'led_movil';
type MediaKind = 'image' | 'video';
type FormState = { name: string; ciudad: 'mendoza' | 'buenos-aires'; tipo_soporte: SupportType; active: boolean; disponibilidad: 'disponible' | 'reservado'; isFeatured: boolean; address: string; lat: string; lng: string; mapa_url: string; description: string; coverUrl: string; coverKind: MediaKind; restMedia: string[]; traditional: { formato: string; medidas: string; caras: string; impresion: string }; led: { formato: string; medidas: string; resolucion: string; frecuencia: string; video_mode: string; spot_duration: string }; mobile: { pantalla: string; resolucion: string; spot_duration: string; minimum_daily_outings: string; recorrido: string; operation_days: string }; pricing: { exhibition: string; installation: string; printing: string; monthly: string; exclusive: string; currency: string }; reservedFrom: string; reservedUntil: string };
const emptyForm: FormState = { name: '', ciudad: 'mendoza', tipo_soporte: 'tradicional', active: true, disponibilidad: 'disponible', isFeatured: false, address: '', lat: '', lng: '', mapa_url: '', description: '', coverUrl: '', coverKind: 'image', restMedia: [], traditional: { formato: '', medidas: '', caras: '', impresion: '' }, led: { formato: '', medidas: '', resolucion: '', frecuencia: '', video_mode: '', spot_duration: '' }, mobile: { pantalla: '', resolucion: '', spot_duration: '', minimum_daily_outings: '', recorrido: '', operation_days: '' }, pricing: { exhibition: '', installation: '', printing: '', monthly: '', exclusive: '', currency: 'ARS' }, reservedFrom: '', reservedUntil: '' };
const inputClass = 'mt-1 w-full rounded-xl border border-gray-200 bg-white px-3.5 py-2.5 text-sm text-gray-900 outline-none transition focus:border-gray-400 focus:ring-2 focus:ring-gray-100';
const labelClass = 'text-[11px] font-bold uppercase tracking-[0.08em] text-gray-500';
const sectionClass = 'rounded-2xl border border-gray-200 bg-white p-5 shadow-2xs';
const familyFor = (type: SupportType): SupportFamily => type === 'led' ? 'led' : type === 'led_movil' ? 'led_mobile' : 'traditional';
const text = (v: unknown) => v == null ? '' : String(v);
const splitPeriod = (v: unknown) => { if (typeof v !== 'string' || !v) return { from: '', until: '' }; const [from, until] = v.split('|'); return { from: from || '', until: until || '' }; };

function normalize(item: any): FormState { const technical = item?.technical || {}; const pricing = item?.pricing || {}; const route = item?.route || {}; const images = Array.isArray(item?.imageUrls) ? item.imageUrls.map(String).filter(Boolean) : []; const period = splitPeriod(item?.availableFrom); const type = (item?.tipo_soporte || 'tradicional') as SupportType; return { ...emptyForm, name: text(item?.name), ciudad: (item?.ciudad || 'mendoza') as FormState['ciudad'], tipo_soporte: type, active: item?.active !== false, disponibilidad: item?.disponibilidad === 'reservado' ? 'reservado' : 'disponible', isFeatured: item?.isFeatured === true, address: text(item?.address), lat: text(item?.lat), lng: text(item?.lng), mapa_url: text(item?.mapa_url), description: text(item?.description), coverUrl: images[0] || '', coverKind: technical?.metadata?.cover_media_type === 'video' ? 'video' : 'image', restMedia: images.slice(1, 3), traditional: { formato: text(technical.formato || technical.format), medidas: text(technical.measures), caras: text(technical.caras), impresion: text(technical.impresion) }, led: { formato: text(technical.formato || technical.format), medidas: text(technical.measures), resolucion: text(technical.resolution), frecuencia: text(technical.daily_frequency), video_mode: text(technical.video_mode), spot_duration: text(technical.spot_duration_seconds) }, mobile: { pantalla: text(technical.pantalla || technical.measures), resolucion: text(technical.resolution), spot_duration: text(technical.spot_duration_seconds), minimum_daily_outings: text(technical.minimum_daily_outings), recorrido: text(route.route_name || route.schedule || technical.summary), operation_days: text(technical.operation_days || route.weekdays) }, pricing: { exhibition: text(pricing.exhibition_price), installation: text(pricing.installation_price), printing: text(pricing.printing_price), monthly: text(pricing.monthly_price), exclusive: text(pricing.exclusive_price), currency: text(pricing.currency || 'ARS') }, reservedFrom: text(item?.reservedFrom || period.from), reservedUntil: text(item?.reservedUntil || period.until) }; }
function payloadFrom(form: FormState) { if (form.disponibilidad === 'reservado' && (!form.reservedFrom || !form.reservedUntil)) throw new Error('Para un soporte reservado, completá Desde y Hasta.'); if (form.disponibilidad === 'reservado' && form.reservedUntil < form.reservedFrom) throw new Error('La fecha Hasta no puede ser anterior a Desde.'); let technical: any; if (form.tipo_soporte === 'tradicional') technical = { summary: form.traditional.formato, measures: form.traditional.medidas, formato: form.traditional.formato, caras: Number(form.traditional.caras || 0), impresion: form.traditional.impresion }; if (form.tipo_soporte === 'led') technical = { summary: form.led.formato, measures: form.led.medidas, formato: form.led.formato, resolution: form.led.resolucion, daily_frequency: form.led.frecuencia, video_mode: form.led.video_mode, spot_duration_seconds: Number(form.led.spot_duration || 0) }; if (form.tipo_soporte === 'led_movil') technical = { summary: form.mobile.pantalla, measures: form.mobile.pantalla, resolution: form.mobile.resolucion, spot_duration_seconds: Number(form.mobile.spot_duration || 0), minimum_daily_outings: Number(form.mobile.minimum_daily_outings || 0), operation_days: form.mobile.operation_days }; technical.metadata = { cover_media_type: form.coverKind }; return { name: form.name.trim(), ciudad: form.ciudad, tipo_soporte: form.tipo_soporte, family: familyFor(form.tipo_soporte), active: form.active, disponibilidad: form.disponibilidad, availableFrom: form.disponibilidad === 'reservado' ? `${form.reservedFrom}|${form.reservedUntil}` : null, isFeatured: form.isFeatured, address: form.address.trim(), lat: form.lat === '' ? null : Number(form.lat), lng: form.lng === '' ? null : Number(form.lng), mapa_url: form.mapa_url.trim(), description: form.description.trim(), imageUrls: [form.coverUrl, ...form.restMedia].map((v) => v.trim()).filter(Boolean).slice(0, 3), technical, pricing: { exhibition_price: Number(form.pricing.exhibition || 0), installation_price: Number(form.pricing.installation || 0), printing_price: Number(form.pricing.printing || 0), monthly_price: Number(form.pricing.monthly || 0), exclusive_price: Number(form.pricing.exclusive || 0), currency: form.pricing.currency }, ...(form.tipo_soporte === 'led_movil' ? { route: { route_name: form.mobile.recorrido, schedule: form.mobile.operation_days, route_mode: 'led_mobile', routePath: [], waypoints: [] } } : {}) }; }
function Field({ label, value, onChange, type = 'text', placeholder }: { label: string; value: string; onChange: (v: string) => void; type?: string; placeholder?: string }) { return <div><label className={labelClass}>{label}</label><input className={inputClass} type={type} placeholder={placeholder} value={value} onChange={(e) => onChange(e.target.value)} /></div>; }

export default function DashboardSupportProductEditor({ mode: explicitMode }: { mode?: 'create' | 'edit' }) {
  const { canonicalId } = useParams(); const navigate = useNavigate(); const mode = explicitMode || (canonicalId ? 'edit' : 'create'); const [form, setForm] = useState<FormState>(emptyForm); const [loading, setLoading] = useState(mode === 'edit'); const [saving, setSaving] = useState(false); const [error, setError] = useState('');
  useEffect(() => { if (mode !== 'edit' || !canonicalId) return; (async () => { try { const token = localStorage.getItem('admin_token'); if (!token) return navigate('/login'); const r = await apiFetch(`/api/admin/supports/${encodeURIComponent(canonicalId)}`, { headers: { Authorization: `Bearer ${token}` } }); const j = await r.json(); if (r.status === 401) return navigate('/login'); if (!r.ok || j.status !== 'success') throw new Error(j.message || 'No se pudo cargar el soporte.'); setForm(normalize(j.data)); } catch (e) { setError(e instanceof Error ? e.message : 'No se pudo cargar el soporte.'); } finally { setLoading(false); } })(); }, [mode, canonicalId, navigate]);
  const theme = useMemo(() => getSupportEditorThemeConfig(form.tipo_soporte), [form.tipo_soporte]); const set = (patch: Partial<FormState>) => setForm((c) => ({ ...c, ...patch })); const setNested = (key: 'traditional' | 'led' | 'mobile' | 'pricing', patch: any) => setForm((c) => ({ ...c, [key]: { ...c[key], ...patch } }));
  const addRest = () => form.restMedia.length < 2 && set({ restMedia: [...form.restMedia, ''] }); const updateRest = (i: number, v: string) => set({ restMedia: form.restMedia.map((x, n) => n === i ? v : x) }); const removeRest = (i: number) => set({ restMedia: form.restMedia.filter((_, n) => n !== i) });
  const previewItem = useMemo<InventoryItem>(() => { const family = familyFor(form.tipo_soporte); const technical: any = form.tipo_soporte === 'tradicional' ? { measures: form.traditional.medidas, summary: form.traditional.formato } : form.tipo_soporte === 'led' ? { measures: form.led.medidas, resolution: form.led.resolucion, daily_frequency: form.led.frecuencia, metadata: { cover_media_type: form.coverKind } } : { measures: form.mobile.pantalla, resolution: form.mobile.resolucion, spot_duration_seconds: Number(form.mobile.spot_duration || 0), minimum_daily_outings: Number(form.mobile.minimum_daily_outings || 0), metadata: { cover_media_type: form.coverKind } }; const base: any = { canonical_id: canonicalId || 'preview', name: form.name || 'Nombre del soporte', ciudad: form.ciudad, tipo_soporte: form.tipo_soporte, family, active: form.active, description: form.description, characteristics: form.tipo_soporte === 'traditional' ? form.traditional.impresion : form.tipo_soporte === 'led' ? form.led.video_mode : form.mobile.recorrido, mapa_url: form.mapa_url, imageUrls: [form.coverUrl, ...form.restMedia].filter(Boolean).slice(0, 3), disponibilidad: form.disponibilidad, availableFrom: form.disponibilidad === 'reservado' ? `${form.reservedFrom}|${form.reservedUntil}` : undefined, reservedFrom: form.disponibilidad === 'reservado' ? form.reservedFrom : undefined, reservedUntil: form.disponibilidad === 'reservado' ? form.reservedUntil : undefined, address: form.address || 'Ubicación del soporte', isFeatured: form.isFeatured, technical }; return family === 'led_mobile' ? { ...base, routePath: [], waypoints: [], schedule: form.mobile.operation_days, duration: form.mobile.recorrido } as MobileRoute : base as InventoryItem; }, [canonicalId, form]);
  const save = async () => { setError(''); if (form.name.trim().length < 2) { setError('Completá el Soporte ID / Nombre.'); return; } setSaving(true); try { const token = localStorage.getItem('admin_token'); if (!token) return navigate('/login'); const url = mode === 'create' ? '/api/admin/supports' : `/api/admin/supports/${encodeURIComponent(canonicalId || '')}`; const r = await apiFetch(url, { method: mode === 'create' ? 'POST' : 'PATCH', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` }, body: JSON.stringify(payloadFrom(form)) }); const j = await r.json(); if (r.status === 401) return navigate('/login'); if (!r.ok || j.status !== 'success') throw new Error(j.message || 'No se pudo guardar el soporte.'); const id = j.data?.canonical_id || j.data?.canonicalId || canonicalId; if (id) navigate(`/dashboard/soportes/${encodeURIComponent(id)}/edit`, { replace: true }); } catch (e) { setError(e instanceof Error ? e.message : 'No se pudo guardar el soporte.'); } finally { setSaving(false); } };
  if (loading) return <DashboardShell><div className="mx-auto max-w-5xl py-20 text-center text-sm text-gray-500">Cargando soporte…</div></DashboardShell>;
  const typeLabel = form.tipo_soporte === 'led_movil' ? 'LED móvil' : form.tipo_soporte === 'led' ? 'LED' : 'Tradicional';
  return <DashboardShell><div className="mx-auto max-w-7xl space-y-5 pb-14"><header className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><button onClick={() => navigate('/dashboard/soportes')} className="mb-2 inline-flex items-center gap-2 text-xs font-bold text-gray-500 hover:text-gray-900"><ArrowLeft className="h-4 w-4"/> Gestión de Soportes</button><div className="text-eyebrow text-emerald-700">{mode==='create'?'ALTA DE PRODUCTO':'EDICIÓN DE PRODUCTO'} · {theme.label}</div><h1 className="mt-2 text-page-title text-gray-900">{mode==='create'?'Nuevo soporte':form.name||'Editar soporte'}</h1><p className="mt-1 text-sm text-gray-500">Inventario → Card → Pricing. La Product Card se actualiza en tiempo real.</p></div><div className="flex flex-wrap gap-2">{canonicalId&&<button onClick={()=>navigate(`/dashboard/soportes/${encodeURIComponent(canonicalId)}/preview`)} className="inline-flex items-center gap-2 rounded-xl border border-gray-200 bg-white px-4 py-2.5 text-xs font-bold text-gray-700"><Eye className="h-4 w-4"/> Preview completa</button>}<button onClick={()=>navigate('/dashboard/soportes')} className="rounded-xl border border-gray-200 bg-white px-4 py-2.5 text-xs font-bold text-gray-700">Cancelar</button><button disabled={saving} onClick={save} className="inline-flex items-center gap-2 rounded-xl bg-gray-900 px-4 py-2.5 text-xs font-bold text-white disabled:opacity-50"><Save className="h-4 w-4"/>{saving?'Guardando…':mode==='create'?'Crear soporte':'Guardar cambios'}</button></div></header>{error&&<div role="alert" className="rounded-xl border border-red-100 bg-red-50 px-4 py-3 text-sm font-semibold text-red-700">{error}</div>}
  <div className="grid items-start gap-5 lg:grid-cols-[minmax(0,1.55fr)_minmax(320px,0.9fr)]"><div className="space-y-5">
    <section className={sectionClass}><div className="flex items-start justify-between gap-3"><div><div className="text-eyebrow text-gray-500">01 · INVENTARIO</div><h2 className="mt-1 text-lg font-bold text-gray-900">Identidad, ubicación y disponibilidad</h2></div><span className="rounded-full bg-gray-100 px-3 py-1 text-[11px] font-bold text-gray-600">{typeLabel}</span></div><div className="mt-5 grid gap-4 md:grid-cols-2"><div><label className={labelClass}>Tipología *</label><select className={inputClass} value={form.tipo_soporte} onChange={(e)=>set({tipo_soporte:e.target.value as SupportType})}><option value="tradicional">Tradicional</option><option value="led">LED</option><option value="led_movil">LED móvil</option></select></div><div><label className={labelClass}>Plaza *</label><select className={inputClass} value={form.ciudad} onChange={(e)=>set({ciudad:e.target.value as FormState['ciudad']})}><option value="mendoza">Mendoza</option><option value="buenos-aires">Buenos Aires</option></select></div><div className="md:col-span-2"><Field label="Soporte ID / Nombre *" value={form.name} onChange={(v)=>set({name:v})}/></div><Field label="Latitud" value={form.lat} onChange={(v)=>set({lat:v})}/><Field label="Longitud" value={form.lng} onChange={(v)=>set({lng:v})}/><div className="md:col-span-2"><Field label="Google Maps URL" value={form.mapa_url} onChange={(v)=>set({mapa_url:v})}/></div><div className="md:col-span-2 rounded-xl border border-gray-200 bg-gray-50 p-4"><div className={labelClass}>Disponibilidad</div><div className="mt-3 flex flex-wrap gap-2"><button type="button" onClick={()=>set({disponibilidad:'disponible',reservedFrom:'',reservedUntil:''})} className={`rounded-xl px-4 py-2.5 text-sm font-bold ${form.disponibilidad==='disponible'?'bg-white text-gray-900 shadow-sm ring-1 ring-gray-200':'text-gray-500'}`}>Disponible</button><button type="button" onClick={()=>set({disponibilidad:'reservado'})} className={`rounded-xl px-4 py-2.5 text-sm font-bold ${form.disponibilidad==='reservado'?'bg-white text-gray-900 shadow-sm ring-1 ring-gray-200':'text-gray-500'}`}>Reservado</button></div>{form.disponibilidad==='reservado'&&<div className="mt-4 grid gap-4 sm:grid-cols-2"><Field label="Desde" type="date" value={form.reservedFrom} onChange={(v)=>set({reservedFrom:v})}/><Field label="Hasta" type="date" value={form.reservedUntil} onChange={(v)=>set({reservedUntil:v})}/></div>}</div><div className="md:col-span-2 rounded-xl border border-gray-200 bg-gray-50 p-4"><div className={labelClass}>Publicación</div><div className="mt-3 flex gap-2"><button type="button" onClick={()=>set({active:true})} className={`rounded-xl px-4 py-2.5 text-sm font-bold ${form.active?'bg-white text-gray-900 shadow-sm ring-1 ring-gray-200':'text-gray-500'}`}>Publicado</button><button type="button" onClick={()=>set({active:false})} className={`rounded-xl px-4 py-2.5 text-sm font-bold ${!form.active?'bg-white text-gray-900 shadow-sm ring-1 ring-gray-200':'text-gray-500'}`}>Archivado</button></div></div></div></section>
    <section className={sectionClass}><div><div className="text-eyebrow text-gray-500">02 · CARD</div><h2 className="mt-1 text-lg font-bold text-gray-900">Contenido publicado</h2></div><div className="mt-5 space-y-5"><div className="rounded-xl border border-gray-200 p-4"><div className="flex items-center justify-between"><div><div className={labelClass}>Portada</div><p className="mt-1 text-xs text-gray-500">Foto o video principal.</p></div>{form.coverKind==='video'?<Video className="h-5 w-5 text-gray-500"/>:<ImagePlus className="h-5 w-5 text-gray-500"/>}</div><div className="mt-4 grid gap-3 sm:grid-cols-[1fr_auto]"><Field label="URL de portada" value={form.coverUrl} onChange={(v)=>set({coverUrl:v})}/><select aria-label="Tipo de portada" className={inputClass.replace('mt-1','mt-5')} value={form.coverKind} onChange={(e)=>set({coverKind:e.target.value as MediaKind})}><option value="image">Foto</option><option value="video">Video</option></select></div></div><div className="rounded-xl border border-gray-200 p-4"><div className="flex items-center justify-between"><div><div className={labelClass}>Resto multimedia</div><p className="mt-1 text-xs text-gray-500">Hasta 2 recursos adicionales para el carousel.</p></div><button type="button" onClick={addRest} disabled={form.restMedia.length>=2} className="rounded-lg border border-gray-200 px-3 py-2 text-xs font-bold text-gray-700 disabled:opacity-50">+ Cargar más</button></div><div className="mt-4 space-y-3">{form.restMedia.map((v,i)=><div className="flex gap-2" key={`${i}-${v}`}><input className={inputClass} value={v} placeholder={`URL multimedia ${i+2}`} onChange={(e)=>updateRest(i,e.target.value)}/><button type="button" onClick={()=>removeRest(i)} className="mt-1 rounded-xl border border-gray-200 px-3 text-gray-500 hover:text-red-600" aria-label="Eliminar multimedia"><Trash2 className="h-4 w-4"/></button></div>)}{!form.restMedia.length&&<p className="text-xs text-gray-400">Sin recursos adicionales.</p>}</div></div><div className="grid gap-4 md:grid-cols-2"><div className="md:col-span-2"><Field label="Ubicación" value={form.address} onChange={(v)=>set({address:v})} placeholder="Ej. San Juan 230"/></div>{form.tipo_soporte==='tradicional'&&<><Field label="Formato" value={form.traditional.formato} onChange={(v)=>setNested('traditional',{formato:v})}/><Field label="Medidas" value={form.traditional.medidas} onChange={(v)=>setNested('traditional',{medidas:v})}/><Field label="Caras" value={form.traditional.caras} onChange={(v)=>setNested('traditional',{caras:v})}/><Field label="Impresión" value={form.traditional.impresion} onChange={(v)=>setNested('traditional',{impresion:v})}/></>}{form.tipo_soporte==='led'&&<><Field label="Formato" value={form.led.formato} onChange={(v)=>setNested('led',{formato:v})}/><Field label="Medidas" value={form.led.medidas} onChange={(v)=>setNested('led',{medidas:v})}/><Field label="Resolución" value={form.led.resolucion} onChange={(v)=>setNested('led',{resolucion:v})}/><Field label="Frecuencia" value={form.led.frecuencia} onChange={(v)=>setNested('led',{frecuencia:v})}/><Field label="Modo de video" value={form.led.video_mode} onChange={(v)=>setNested('led',{video_mode:v})}/><Field label="Duración de spot" value={form.led.spot_duration} onChange={(v)=>setNested('led',{spot_duration:v})}/></>}{form.tipo_soporte==='led_movil'&&<><Field label="Pantalla / formato" value={form.mobile.pantalla} onChange={(v)=>setNested('mobile',{pantalla:v})}/><Field label="Resolución" value={form.mobile.resolucion} onChange={(v)=>setNested('mobile',{resolucion:v})}/><Field label="Duración de spot" value={form.mobile.spot_duration} onChange={(v)=>setNested('mobile',{spot_duration:v})}/><Field label="Salidas mínimas" value={form.mobile.minimum_daily_outings} onChange={(v)=>setNested('mobile',{minimum_daily_outings:v})}/><Field label="Recorrido" value={form.mobile.recorrido} onChange={(v)=>setNested('mobile',{recorrido:v})}/><Field label="Días de operación" value={form.mobile.operation_days} onChange={(v)=>setNested('mobile',{operation_days:v})}/></>}</div><div><label className={labelClass}>Descripción · máximo 250 caracteres</label><textarea className={`${inputClass} min-h-28`} maxLength={250} value={form.description} onChange={(e)=>set({description:e.target.value})}/><div className="mt-1 text-right text-[11px] text-gray-400">{form.description.length}/250</div></div><div className="flex items-center justify-between rounded-xl border border-gray-200 px-4 py-3"><div className="text-sm font-bold text-gray-900">¿Publicar como destacado?</div><button type="button" onClick={()=>set({isFeatured:!form.isFeatured})} className={`rounded-full px-4 py-2 text-xs font-bold ${form.isFeatured?'bg-gray-900 text-white':'bg-gray-100 text-gray-600'}`}>{form.isFeatured?'Sí':'No'}</button></div></div></section>
    <section className={sectionClass}><div><div className="text-eyebrow text-gray-500">03 · PRICING INTERNO</div><h2 className="mt-1 text-lg font-bold text-gray-900">Precios comerciales</h2></div><div className="mt-5 grid gap-4 md:grid-cols-2"><Field label="Exhibición" value={form.pricing.exhibition} onChange={(v)=>setNested('pricing',{exhibition:v})}/><Field label="Instalación" value={form.pricing.installation} onChange={(v)=>setNested('pricing',{installation:v})}/>{form.tipo_soporte==='tradicional'&&<Field label="Impresión" value={form.pricing.printing} onChange={(v)=>setNested('pricing',{printing:v})}/>}<Field label="Mensual" value={form.pricing.monthly} onChange={(v)=>setNested('pricing',{monthly:v})}/>{form.tipo_soporte!=='tradicional'&&<Field label="Exclusivo" value={form.pricing.exclusive} onChange={(v)=>setNested('pricing',{exclusive:v})}/>}<Field label="Moneda" value={form.pricing.currency} onChange={(v)=>setNested('pricing',{currency:v})}/></div></section>
  </div><aside className="lg:sticky lg:top-6"><div className="rounded-2xl border border-gray-200 bg-white p-4 shadow-sm"><div className="mb-4 flex items-center justify-between"><div><div className="text-eyebrow text-gray-500">PREVIEW</div><h2 className="mt-1 text-lg font-bold text-gray-900">Product Card</h2></div><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wide text-emerald-700">Tiempo real</span></div><div className="rounded-xl bg-gray-50 p-3"><SupportCard item={previewItem} variant="catalog" /></div><p className="mt-3 text-xs leading-relaxed text-gray-500">La preview usa la misma Product Card del inventario. Las fechas sólo aparecen cuando el soporte está Reservado.</p></div></aside></div></div></DashboardShell>;
}
